---
title: "스프링 핵심원리 9. 빈 스코프"
date: 2023-02-12 07:59:45 +0000
categories: ["Spring", "핵심원리"]
tags: ["핵심원리", "spring"]
math: true
permalink: /posts/saction-9-빈-스코프/
---

#### 9장 - 빈 스코프

[9. 빈 스코프.pdf](https://s3-us-west-2.amazonaws.com/secure.notion-static.com/33b5c82c-3a48-42a9-b37c-ddf0e8d3d3f6/9._%EB%B9%88_%EC%8A%A4%EC%BD%94%ED%94%84.pdf)

> 💡 빈 스코프 : 빈이 존재할 수 있는 범위

#### 스프링에서 지원하는 스코프

- 싱글톤 : 기본 스코프, 스프링 컨테이너의 시작부터 끝까지 유지되는 가장 넓은 범위의 스코프

- 프로토타입 : 스프링 컨테이너는 프로토타입 빈의 생성, 의존관계 주입까지만 관여하고 더는 관리하지 않는 매우 짧은 범위의 스코프 → 종료 메소드(Destroy method) 호출 안 됨.

- 웹 관련 스코프
    - request : 웹 요청이 들어오고 나갈때 까지 유지되는 스코프

    - session : 웹 세션이 생성되고 종료될때까지 유지되는 스코프

    - application : 웹의 서블릿 컨텍스트와 같은 범위로 유지되는 스코프

---

#### 싱글톤 빈 조회

> 💡 싱글톤으로 빈을 등록할 시, 해당 빈은 하나의 인스턴스만이 생성된다.
> 때문에 몇 번을 조회하더라도 다 같은 빈을 공유하는 것을 알 수 있다.

{% raw %}
```java
public class SingletonTest {

    @Test
    void singletonBeanFind() {

        // 클래스를 직접 등록하게 되면 해당 클래스가 컴포넌트 스캔의 대상이 되어 빈으로 자동 등록된다.(@Component 필요 없음)
        AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(SingletonBean.class);

        SingletonBean singletonBean1 = ac.getBean(SingletonBean.class);
        SingletonBean singletonBean2 = ac.getBean(SingletonBean.class);

        System.out.println("singletonBean1 = " + singletonBean1);
        System.out.println("singletonBean2 = " + singletonBean2);
        assertThat(singletonBean1).isSameAs(singletonBean2);

        ac.close();

    }

		@Scope("singleton")
    static class SingletonBean {

        @PostConstruct
        public void init() {
            System.out.println("SingletonBean.init");
        }

        @PreDestroy
        public void destroy() {
            System.out.println("SingletonBean.destroy");
        }

    }

}
```
{% endraw %}

{% raw %}
```java
// 테스트 결과
SingletonBean.init
singletonBean1 = hello.core.scope.SingletonTest$SingletonBean@7e990ed7
singletonBean2 = hello.core.scope.SingletonTest$SingletonBean@7e990ed7
17:21:23.250 [main] DEBUG org.springframework.context.annotation.AnnotationConfigApplicationContext - Closing org.springframework.context.annotation.AnnotationConfigApplicationContext@7334aada, started on Sun Feb 12 17:21:23 KST 2023
SingletonBean.destroy
```
{% endraw %}

> `@Scope("singleton")` 으로 등록 시,   
> `@PostConstruct` 와 `@PreDestroy` 메소드가 모두 잘 동작하는 것을 볼 수 있다.

---

#### 프로토타입 빈 스코프

![image](/assets/images/2bfef5f8da21477a81273f8ad158fd93.png)

![image](/assets/images/a70845681a57412e99e97e57df3737dd.png)

> 💡 프로토타입으로 빈을 등록하게 되면, 클라이언트가 스프링 컨테이너에 프로토타입 빈을 요청하는 시점에 프로토타입 빈을 생성하고, 필요한 의존관계를 주입한다.
> 그리고 생성한 프로토타입 빈을 클라이언트에게 반환한 뒤, 스프링 컨테이너는 더이상 해당 빈을 관리하지 않는다.
> 이후 프로토타입 빈을 스프링 컨테이너에 다시 요청하게 되면 컨테이너는 다시 새로운 빈을 생성한 뒤 반환한다.
>
> > 핵심 :** 스프링 컨테이너는 프로토타입 빈을 생성하고, 의존관계 주입, 초기화까지만 처리한다.** → `@preDestroy` 메소드는 호출되지 않는다.

{% raw %}
```java
public class PrototypeTest {

    @Test
    void prototypeBeanFind() {

        AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(PrototypeBean.class);

        System.out.println("find prototypeBean1");
        PrototypeBean prototypeBean1 = ac.getBean(PrototypeBean.class);
        System.out.println("find prototypeBean2");
        PrototypeBean prototypeBean2 = ac.getBean(PrototypeBean.class);

        System.out.println("prototypeBean1 = " + prototypeBean1);
        System.out.println("prototypeBean2 = " + prototypeBean2);

        assertThat(prototypeBean1).isNotSameAs(prototypeBean2);

        ac.close();

    }

    @Scope("prototype")
    static class PrototypeBean {

        @PostConstruct
        public void init() {
            System.out.println("PrototypeBean.init");
        }

        @PreDestroy
        public void destroy() {
            System.out.println("PrototypeBean.destroy");
        }

    }
}
```
{% endraw %}

{% raw %}
```java
// 테스트 결과

find prototypeBean1
PrototypeBean.init
find prototypeBean2
PrototypeBean.init
prototypeBean1 = hello.core.scope.PrototypeTest$PrototypeBean@7e990ed7
prototypeBean2 = hello.core.scope.PrototypeTest$PrototypeBean@c05fddc
17:26:47.275 [main] DEBUG org.springframework.context.annotation.AnnotationConfigApplicationContext - Closing org.springframework.context.annotation.AnnotationConfigApplicationContext@7334aada, started on Sun Feb 12 17:26:47 KST 2023
```
{% endraw %}

> `@Scope("prototype")` 으로 등록 시,   
> `@PostConstruct` 메소드는 동작하지만 `@PreDestroy` 메소드는 호출되지 않는다.  
> prototypeBean1, 2 는 다른 빈이기 때문에 init 메소드는 각각 호출되는 것을 확인할 수 있음.

> 💡 프로토타입 빈의 특징
>
> - 스프링 컨테이너에 요청할 때 마다 새로 생성된다.
>
> - 스프링 컨테이너는 프로토타입 빈의 생성, 의존관계 주입, 초기화까지만 관여한다.
>
> - 종료 메서드가 호출되지 않는다. 그래서 프로토타입 빈은 프로토타입 빈을 조회한 클라이언트가 관리해야 한다.  종료 메서드에 대한 호출도 클라이언트가 직접 해야한다.

---

#### 프로토타입 스코프를 싱글톤 빈과 함께 사용 시 문제점

> 💡 싱글톤 빈 내에서 프로토타입 빈을 사용하게 될 때 문제가 발생할 수 있다.

> 개발자의 의도 : 사용자가 클라이언트 빈을 사용할 때 프로토타입 빈을 사용 시마다 생성해서 사용하고 싶어 한다.

{% raw %}
```java
public class SingletonWithPrototypeTest1 {

    @Test
    void prototypeFind() {

        AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(PrototypeBean.class);
        PrototypeBean prototypeBean1 = ac.getBean(PrototypeBean.class);
        prototypeBean1.addCount();
        assertThat(prototypeBean1.getCount()).isEqualTo(1); // 사용자의 의도 = 1

        PrototypeBean prototypeBean2 = ac.getBean(PrototypeBean.class);
        prototypeBean2.addCount();
        assertThat(prototypeBean2.getCount()).isEqualTo(1);// 사용자의 의도 = 1

    }

    /**
     * clientBean 은 PrototypeBean 을 주입받아 사용한다. 이 때의 문제점을 파악한다.
     * 사용자(client1, client2) 는 프로토타입 빈을 사용하는 자신의 카운트가 자신이 올린 만큼만 올라갈 것이라고 생각하지만,
     * 실제로 prototypeBean 의 클라이언트는 ClientBean 이기 때문에 원하는 대로 동작하지 않는다는 것이 문제.
     */
    @Test
    void singletonClientUsePrototype() {

        AnnotationConfigApplicationContext ac = new AnnotationConfigApplicationContext(PrototypeBean.class, ClientBean.class);

        ClientBean clientBean1 = ac.getBean(ClientBean.class);
        int count1 = clientBean1.logic();
        assertThat(count1).isEqualTo(1);

        ClientBean clientBean2 = ac.getBean(ClientBean.class);
        int count2 = clientBean2.logic();
        assertThat(count2).isEqualTo(2);
    }

    @Scope("singleton")
    static class ClientBean {
        private final PrototypeBean prototypeBean;

        @Autowired
        ClientBean(PrototypeBean prototypeBean) {
            this.prototypeBean = prototypeBean;
        }

        public int logic() {
            prototypeBean.addCount();
            return prototypeBean.getCount();
        }
    }

    @Scope("prototype")
    static class PrototypeBean {

        private int count=0;

        public void addCount() {
            count++;
        }

        public int getCount() {
            return count;
        }

        @PostConstruct
        public void init() {
            System.out.println("prototypeBean.init : " + this);
        }

        @PreDestroy
        public void destroy() {
            System.out.println("prototypeBean.destroy");
        }

    }
}
```
{% endraw %}

{% raw %}
```java
// 테스트 결과
org.opentest4j.AssertionFailedError: 
expected: 1
 but was: 2
필요:1
실제   :2
```
{% endraw %}

> 결과가 실패할 수밖에 없다. client2의 count는 1이 아니라 2가 되야 하기 때문.  
>   
> 여기서 중요한 점이 있는데, `clientBean`이 내부에 가지고 있는 프로토타입 빈은 이미 과거에 주입이 끝난 빈이다. 주입 시점에 스프링 컨테이너에 요청해서 프로토타입 빈이 새로 생성이 된 것이지, 사용 할 때마다 새로 생성되는 것이 아니다! → 해결해야 할 중요 사안.

> 💡 위의 코드에서는 `ClientBean` 이 생성될 때 `PrototypeBean` 을 생성하여 주입받는다. 
> 이 때 최초 1회만 주입받는다는 것이 문제인 것. 개발자의 의도와는 다른 것이다.
> 이렇게 되면 싱글톤 빈과 프로토타입 빈의 생성 및 유지기간이 같아지는 문제가 발생한다.

---

#### Provider을 통해 싱글톤과 프로토타입을 함께 사용시 문제점을 해결

> 💡 스프링의 Provider 기능을 사용하여 프로토타입 빈을 우리가 원하는 대로 매 번 새로 생성되도록 할 수 있다.

> 의존관계를 외부에서 주입(DI) 받는게 아니라 직접 필요한 의존관계를 찾는 것을 Dependency Lookup(DL), 의존관계 조회(탐색) 이라고 한다.

{% raw %}
```java
@Scope("singleton")
    static class ClientBean {

        // 스프링의 ObjectProvider 를 이용해서 항상 새로운 프로토타입 빈이 생성되도록 할 수 있다.
        // DL(Dependency Lookup) 사용.
        @Autowired
        private ObjectProvider<PrototypeBean> prototypeBeanProvider;

        public int logic() {

            // 스프링 컨테이너 내부에서 해당 빈을 찾아서 반환해준다.
            PrototypeBean prototypeBean = prototypeBeanProvider.getObject();

            prototypeBean.addCount();
            return prototypeBean.getCount();
        }
    }
```
{% endraw %}

> 💡 위 코드로 clientBean을 수정한 뒤, 테스트를 실행하면 우리가 원하는 대로 client1, 2 모두 count의 결과값이 1로 나오는 것을 확인할 수 있다.
>
> 하지만 스프링이 지원해주는 ObjectProvider 을 사용할 시, 스프링에 의존적인 코드가 된다는 단점을 가지고 있다.

#### JSR-330 Provider 사용

> 💡 스프링에 의존적이지 않게 DL을 사용하는 방법
> javax.inject.Provider 라는 JSR-330 자바 표준을 사용한다.
> 별도의 라이브러리 필요 : `implementation 'javax.inject:javax.inject:1'`

{% raw %}
```java
@Scope("singleton")
    static class ClientBean {

        // javax.inject.Provider 사용
        // DL(Dependency Lookup) 기능 제공
        // 스프링에 의존적이지 않음(자바 표준 사용)
        @Autowired
        private Provider<PrototypeBean> provider;


        public int logic() {

            // 스프링 컨테이너 내부에서 해당 빈을 찾아서 반환해준다.
            PrototypeBean prototypeBean = provider.get();

            prototypeBean.addCount();
            return prototypeBean.getCount();
        }
    }
```
{% endraw %}

> 💡 실행하면 `provider.get()`에 의해 매번 새로운 프로토타입 빈이 생성되는 것을 확인할 수 있다. 
> `Provider.get()` 은 스프링 컨테이너에서 해당하는 빈을 찾아서 반환하는 DL을 수행하다.
> 간단하게 DL 만을 위해서 자바 표준이 만들어진 것이기 때문에 단순하고 Mock 등의 테스트 코드를 만드는 데에도 장점이 있다.

---

#### 웹 스코프

- 웹 스코프의 특징
    - 웹 스코프는 웹 환경에서만 동작한다.

    - 웹 스코프는 프로토타입과 다르게 스프링이 해당 스코프의 종료시점까지 관리한다. 따라서 종료메서드가 호출된다.

> 💡 웹 스코프 종류.
>
> - request: HTTP 요청 하나가 들어오고 나갈 때 까지 유지되는 스코프, 각각의 HTTP 요청마다 별도의 빈 인스턴스가 생성되고, 관리된다.
>
> - session: HTTP Session과 동일한 생명주기를 가지는 스코프
>
> - application: 서블릿 컨텍스트( ServletContext )와 동일한 생명주기를 가지는 스코프
>
> - websocket: 웹 소켓과 동일한 생명주기를 가지는 스코프

> 💡 참고: 스프링 부트는 웹 라이브러리가 없으면 우리가 지금까지 학습한
> _**AnnotationConfigApplicationContext**_** **을 기반으로 애플리케이션을 구동한다. 웹 라이브러리가 추가되면 웹과 관련된 추가 설정과 환경들이 필요하므로
>
> > _**AnnotationConfigServletWebServerApplicationContext **_를 기반으로 애플리케이션을 구동한다.

- 예제 코드

MyLogger Class

{% raw %}
```java
@Component
@Scope(value = "request")
public class MyLogger {
		//스프링에 HTTP 요청이 들어오게 되면 요청 당 빈이 하나씩 생성되고, 요청 끝나는 시점에 소멸
     
    private String uuid;
    private String requestURL;

    public void setRequestURL(String requestURL) {
        this.requestURL = requestURL;
    }

    public void log(String message) {
        System.out.println("[" + uuid + "] " + "[" + requestURL + "] " + message);
    }

    @PostConstruct
    public void init() {
        uuid = UUID.randomUUID().toString();
        System.out.println("[" + uuid + "] " + "request scope bean create : " + this);
    }

    @PreDestroy
    public void close() {
        System.out.println("[" + uuid + "] " + "request scope bean close : " + this);
    }
}
```
{% endraw %}

Controller Class

{% raw %}
```java
@Controller
@RequiredArgsConstructor
public class LogDemoController {

    private final LogDemoService logDemoService;
    private final ObjectProvider<MyLogger> myLoggerProvider;

    @RequestMapping("log-demo")
    @ResponseBody // 웹 화면에 리턴 값을 그대로 문자열로 보여주기 위함
    public String logDemo(HttpServletRequest request) {

        MyLogger myLogger = myLoggerProvider.getObject();
        String requestURL = request.getRequestURL().toString();

        myLogger.setRequestURL(requestURL);
        myLogger.log("controller test");
        logDemoService.logic("testId");

        return "OK";
    }

}
```
{% endraw %}

Service Class

{% raw %}
```java
@Service
@RequiredArgsConstructor
public class LogDemoService {

    private final ObjectProvider<MyLogger> myLoggerProvider;

    public void logic(String id) {
        MyLogger myLogger = myLoggerProvider.getObject();
        myLogger.log("service id = " + id);
    }
}
```
{% endraw %}

{% raw %}
```java
// 콘솔 실제 로그(/log-demo 페이지 접속 시)
[a59d2f4b-6522-4ed5-a2c4-3b0154ceb6aa] request scope bean create : hello.core.common.MyLogger@2a772bd1
[a59d2f4b-6522-4ed5-a2c4-3b0154ceb6aa] [http://localhost:9090/log-demo] controller test
[a59d2f4b-6522-4ed5-a2c4-3b0154ceb6aa] [http://localhost:9090/log-demo] service id = testId
[a59d2f4b-6522-4ed5-a2c4-3b0154ceb6aa] request scope bean close : hello.core.common.MyLogger@2a772bd1
[c9ec57d0-ae77-4f13-954d-5ce59fa71787] request scope bean create : hello.core.common.MyLogger@50455993
[c9ec57d0-ae77-4f13-954d-5ce59fa71787] [http://localhost:9090/log-demo] controller test
[c9ec57d0-ae77-4f13-954d-5ce59fa71787] [http://localhost:9090/log-demo] service id = testId
[c9ec57d0-ae77-4f13-954d-5ce59fa71787] request scope bean close : hello.core.common.MyLogger@50455993
```
{% endraw %}

중요하게 봐야 할 점!

- MyLogger 클래스의 빈은 스코프가 request 이다. → HTTP Request 요청이 들어올 때 빈이 생성되고, 요청 종료 시까지 유지된다 → 생성, 소멸 메소드 모두 동작한다. → 때문에 콘솔의 실제 로그를 보면 하나의 request 에 모두 같은 UUID 로 시작 ~ 종료시까지 유지되는 것을 확인할 수 있다.

- Controller, Service 클래스를 보면, MyLogger 빈의 주입 시기를 request 요청 시로 늦추기 위해 provider 를 사용하는 것을 볼 수 있다. 
    - provider 를 사용하지 않고 MyLogger 클래스를 바로 주입받으려 한다면, request 가 생성되지 않아 아직 빈도 생성되지 않은 상태에서 의존성을 주입받으려 해 active 되지 않은 빈을 주입받으려 한다는 오류가 발생한다. 이 때문에 provider 를 사용하는 것을 꼭 인지할 것.

    - _**근데 그것도 프록시 쓰면 안 써도 됨ㅋ**_

#### 스코프와 프록시

MyLogger 클래스에 `@Scope` 를 다음과 같이 수정하자.

{% raw %}
```java
@Component
@Scope(value = "request", proxyMode = ScopedProxyMode.TARGET_CLASS)
public class MyLogger {
....

// 콘솔 로그 - CGLIB 라이브러리를 통한 프록시 객체 생성을 확인할 수 있다.
myLogger = class hello.core.common.MyLogger$$EnhancerBySpringCGLIB$$f2cf79d
[e2d90092-71e8-40c8-a18f-e98e7c971dbc] request scope bean create : hello.core.common.MyLogger@16f58b84
[e2d90092-71e8-40c8-a18f-e98e7c971dbc] [http://localhost:9090/log-demo] controller test
[e2d90092-71e8-40c8-a18f-e98e7c971dbc] [http://localhost:9090/log-demo] service id = testId
[e2d90092-71e8-40c8-a18f-e98e7c971dbc] request scope bean close : hello.core.common.MyLogger@16f58b84
```
{% endraw %}

이렇게 하면, Controller, Service 클래스에서 provider 를 사용하지 않고, MyLogger 클래스를 직접 바로 주입받아 사용할 수 있다.

원래는 안 된다. request scope 의 빈은 실제 request 시점에 생성되기 때문에. 근데 어떻게 가능 ?

> 💡 코드상으로 보면 기존의 오류로 동작하지 않았던 것과 같아 보이지만,
>  MyLogger 클래스에 ProxyMode를 설정하게 되면서 정상적으로 실행되는 것을 볼 수 있다.
>
> 콘솔 로그를 통해 `myLogger` 클래스를 직접 확인하면 알 수 있지만,
> 프록시모드를 사용하게 되면 `MyLogger` 클래스의 실제 기능을 동작시키기 전에는
>  CGLIB 을 통해 MyLogger 의 **가짜 프록시 클래스**를 생성하고, request가 들어오기 전에는
> _**가짜 코드로 의존성을 주입**_하여 의존성 주입의 문제를 해결하는 것이다.
> _다형성을 구현한 것!_
>
> 프록시 객체에는 실제 객체를 찾아주는 로직이 들어 있고,
> 클라이언트가 myLogger.logic 을 호출하면 그것은 프록시 객체의 logic()을 호출하는 것이다.
> 가짜 프록시 객체는 request 스코프의 원래 메소드를 호출한다.

---

- 결론적으로 , provider 또는 proxy 를 통해 객체의 실제 조회를 필요한 시점까지 지연시킬 수 있다는 것.

- 다형성을 통해서 프록시 객체를 생성할 수 있다는 점 또한 중요하게 생각한다.
